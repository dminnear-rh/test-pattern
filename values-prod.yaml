clusterGroup:
  name: prod

  namespaces:
    openshift-nfd:

  sharedValueFiles:
    - '/overrides/values-{{ $.Values.global.accelerator }}.yaml'

  subscriptions:
    nfd:
      name: nfd
      namespace: openshift-nfd

  applications: {}
